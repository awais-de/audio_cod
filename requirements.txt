# Neural Audio Codec — Python 3.8+

# Deep learning
torch>=2.0.0
torchaudio>=2.0.0       # MelSpectrogram (losses.py), audio resampling (train.py, 03b eval)

# Scientific computing
numpy>=1.24.0           # numpy 2.x is supported
scipy>=1.10.0           # transitive dep of pesq / pystoi

# Audio I/O
soundfile>=0.12.1
av>=12.0.0              # PyAV — AAC encode/decode comparison in 03b_phaseC_eval.py

# Configuration
pyyaml>=6.0

# Progress bars
tqdm>=4.66.0

# Evaluation metrics — inference degrades gracefully if either is unavailable
pystoi>=0.3.3           # pure Python, installs everywhere
# pesq requires Python development headers (Python.h) to build from source:
#   Ubuntu/Debian: sudo apt install python3-dev
#   macOS:         headers included with Python — no extra step
#   Windows:       headers included with the official Python installer
pesq>=0.0.4

# ── Optional ──────────────────────────────────────────────────────────────────
#
# PyAudio — only required for real-time streaming (scripts/archive/infer_stream.py)
# Needs the PortAudio system library installed first:
#   Linux:   sudo apt install libportaudio2
#   macOS:   brew install portaudio
#   Windows: pip install pipwin && pipwin install pyaudio
# Then: pip install PyAudio
#
# ── GPU installation ──────────────────────────────────────────────────────────
#
# CUDA 12.1:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# CUDA 11.8:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# CPU only (no CUDA):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
